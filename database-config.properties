# PostgreSQL Database Configuration for Clinic System
# Update these settings to match your database setup

# Database Connection Settings
db.host=localhost
db.port=5432
db.name=clinic_db
db.username=postgres
db.password=your_password_here

# Connection URL (automatically constructed)
# jdbc:postgresql://localhost:5432/clinic_db

# Instructions:
# 1. Update db.username and db.password with your PostgreSQL credentials
# 2. If your database is on a different host/port, update those as well
# 3. Make sure your PostgreSQL server is running
# 4. Ensure the clinic_db database exists with the required tables

# Required Tables:
# - doctors (id, name, specialization, phone)
# - appointments (id, patient_name, patient_phone, doctor_name, appointment_date, appointment_time, status, notes)

# Sample doctors should be inserted as shown in your SQL comments